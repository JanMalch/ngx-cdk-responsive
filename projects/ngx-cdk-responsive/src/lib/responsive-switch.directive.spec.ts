import { ResponsiveSwitchDirective } from './responsive-switch.directive';

describe('ResponsiveSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveSwitchDirective();
    expect(directive).toBeTruthy();
  });
});
